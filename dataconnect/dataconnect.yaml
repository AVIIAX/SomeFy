specVersion: "v1beta"
serviceId: "musicstream"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "musicstream-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
